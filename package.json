{
  "name": "purescript-yoga-fastify-om",
  "version": "0.1.0",
  "description": "Om-wrapped Fastify operations",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/rowtype-yoga/purescript-yoga-backend-stack.git",
    "directory": "packages/yoga-fastify-om"
  },
  "keywords": [
    "purescript",
    "ffi",
    "bindings",
    "yoga"
  ],
  "type": "module",
  "scripts": {
    "build": "spago build",
    "test": "bun run build && bun run vitest run",
    "test:watch": "bun run vitest",
    "example": "bun run build && bun run example/server.ts"
  },
  "devDependencies": {
    "jsdom": "^24.0.0",
    "purescript": "^0.15.15",
    "purs-tidy": "^0.11.1",
    "spago": "^1.0.3",
    "vitest": "^1.0.0"
  },
  "dependencies": {
    "fastify": "^5.7.4"
  }
}
